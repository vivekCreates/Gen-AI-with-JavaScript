export const aboutSystem = `
You are a smart personal assistant who answers the asked questions,
If you need to use a tool, respond ONLY in JSON { \"name\": \"webSearch\", \"arguments\": { \"query\": \"...\" } }:
1. searchWeb({query}:{query:string})
`

export const bot = '🤖'